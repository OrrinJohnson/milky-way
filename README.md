1. Clone the project
2. `cd` into the milky-way directory
3. serve HTML file with your preferred tool of choice. I used ```python3 -m http.server``` which defaults to port 8000
4. open on localhost:<whichever port you specified>
